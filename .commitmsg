feat: launch-ready paper trading MVP

- Move all secrets to environment variables (InsForge, JWT, DB)
- Add .env.example templates for backend and frontend
- Replace hardcoded news with live scraper (ShareSansar + MeroLagani)
- Add NEPSE tiered brokerage fees (0.24-0.36% + SEBON + DP charge)
- Server-side trade validation (10-share lot size, market hours)
- Trading hours enforcement (Sun-Thu 11:00-15:00 NPT)
- Label simulated fundamentals and AI forecasts with disclaimers
- Fix broken scheduler (set_cached_market to set_cached_market_summary)
- Add rate limiting middleware (per-IP sliding window)
- Add WebSocket connection cap (500 max)
- Add React ErrorBoundary for crash recovery
- Frontend trade security: RPC-first with client fallback
- Fix Docker compose (required env vars, no duplicate volumes)
- Add test suite: 14 tests (fees, market hours, news, config)
- Update all README files with accurate documentation
- Add frontend pages: Market, News, IPO, Profile, Reports, etc
- Add Zustand store, InsForge SDK integration, auth flows
